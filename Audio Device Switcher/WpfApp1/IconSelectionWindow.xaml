<Window x:Class="AudioDeviceSwitcher.IconSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Device Icon" Height="400" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F8F9FA"
        Icon="/Resources/Icon.ico">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Select Device Icon" 
                   FontSize="16" FontWeight="SemiBold" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,10"/>

        <!-- Device name -->
        <Border Grid.Row="1" Background="#E8F4FD" BorderBrush="#B3D9FF" BorderThickness="1" 
                CornerRadius="5" Padding="10" Margin="0,0,0,15">
            <TextBlock Name="DeviceNameText" FontWeight="SemiBold" 
                       Foreground="#2C3E50"/>
        </Border>

        <!-- Icon selection grid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" 
                CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <WrapPanel Name="IconSelectionPanel" 
                          Orientation="Horizontal"/>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="ResetButton" Content="Auto Detect" 
                    Width="90" Height="32" Margin="0,0,10,0" 
                    FontSize="13"
                    Click="OnResetClicked"/>

            <Button Name="CancelButton" Content="Cancel" 
                    Width="80" Height="32" Margin="0,0,10,0" 
                    FontSize="13"
                    Click="OnCancelClicked" IsCancel="True"/>

            <Button Name="OkButton" Content="OK" 
                    Width="80" Height="32" 
                    FontSize="13" FontWeight="Medium"
                    Click="OnOkClicked" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>